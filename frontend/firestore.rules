rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read for essential settings (Terms, Privacy, etc.)
    match /settings/{docId} {
      allow read: if true;
      allow write: if request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    // Products and Categories should be publicly readable for shopping
    match /products/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    match /categories/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /vouchers/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Users collection: Allow reading for phone existence checks
    match /users/{userId} {
      allow read: if true; 
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Default rule for everything else: must be authenticated
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
